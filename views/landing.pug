doctype html
html(lang='en')
  include common/head.pug
  +head("Doin some stuff")
  body
    header
      include common/navbar.pug
      +navbar(user)
      .container
        .row.align-items-center
          .col-md-6.order-md1.text-center.text-md-left.pr-md-5
            h1.mb-3 🌲🌲🌲🌲🌲
            p.lead Hellooo,
            p.lead.mb-4 Woooorldddd
            form(action="/" method="POST")
              .input-group.mb-3
                input(name="lead_email", type="email", placeholder="Enter your email address").form-control
                .input-group-append
                  button(type="submit").btn.btn-primary Get Started
            if error
              h1(style="color:red")=error
  include common/footer.pug
  +footer()
  script(src='/javascript/leads.js')